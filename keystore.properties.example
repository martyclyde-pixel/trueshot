# Copy to keystore.properties (which is git-ignored) and fill in.
# app/build.gradle.kts reads this file to configure release signing.
# If the file is absent, release builds simply go out unsigned.

storeFile=upload-keystore.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
